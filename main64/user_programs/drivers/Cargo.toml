[package]
name = "drivers_user_program"
version = "0.1.0"
edition = "2021"

[features]
kernel = []

[dependencies]
lib_driver = { path = "../../lib_driver" }
lib_kaos = { path = "../../lib_kaos" }

[[bin]]
name = "drivers"
path = "src/main.rs"
bench = false
